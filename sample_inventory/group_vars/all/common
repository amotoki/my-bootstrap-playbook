# If set, proxy is configured
#proxy:
#  scheme: http
#  host: proxy_host
#  port: 8080
#no_proxy: 127.0.0.0/8,192.168.0.0/16,172.16.0.0/12,10.0.0.0/8,.example.net

# If set, debian/ubuntu apt proxy will be configured
#deb_proxy: http://172.16.18.32:8000/

# If set, the downloaded tarball is extracted into /opt/stack
#gitrepo_tarball_url: http://172.16.18.32/openstack/openstack-repo.tar.bz2

# It should be set to false if direct SSH to the internet
# is unreachable (default: true)
#use_git_insteadof: false

# Setup OpenStack related settings in myenv role.
# (default: true)
#myenv_openstack_support: false

# If a target host is a hypervisor host, it is useful
# to use a different type of bash prompt. (Used in myenv role)
#hypervisor_host: true

# Set yet if you don't want to override /etc/hosts (default: yes)
#skip_hosts: yes

# If set, sudo NOPASSWD entry is set up for the target user.
#sudo_nopass: yes
